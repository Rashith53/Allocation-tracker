# Cap Split PWA

Publish this folder on an HTTPS host. Then open the HTTPS URL in Chrome on Android and choose **⋮ → Install app** or **Add to Home screen**.

The app keeps its existing localStorage data and includes a PWA manifest, service worker and icons.
